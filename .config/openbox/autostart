#
# These things are run when an Openbox X Session is started.
# You may place a similar script in $HOME/.config/openbox/autostart
# to run user-specific things.
#

# If you want to use GNOME config tools...
#
#if test -x /usr/libexec/gnome-settings-daemon >/dev/null; then
#  /usr/libexec/gnome-settings-daemon &
#elif which gnome-settings-daemon >/dev/null 2>&1; then
#  gnome-settings-daemon &
#fi

# If you want to use XFCE config tools...
#
# xsettingsd &
# xfdesktop &

killall -q sxhkd
killall -q juiced

killall -q tint2
while pgrep -u $UID -x tint2 >/dev/null; do sleep 1; done

# battery monitor
juiced -d

xrdb -l "$HOME/.Xresources" &
xmodmap "$HOME/.Xmodmap" &
numlockx on &

# background and wallpaper
xsetroot -solid darkblue &
# fbsetbg -c -r "$HOME/wallpaper/" &
"$HOME/.fehbg" &

# panel system info
# conky
# (sleep 4s && conky -dc "$HOME/conky/conkyhorizrc") &

#polybar
#(sleep 2s && "$HOME/bin/obxbar.sh") &

# tint2
(sleep 2s && "$HOME/bin/startpanel.sh") &

exit 0;

