# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# autostart  if not in $HOME/.Xprofile ======================================================

# set wallpaper
exec fbsetbg -c -r "$HOME/wallpaper/" &

# comment out if using the i3bar
# exec_always --no-startup-id "$HOME/.config/i3/i3polybar.sh"

# connman network gui
#exec cmst -m -i Adwaita &

# i3 configuration ================================================

set $mod Mod4
set $ALT Mod1

default_orientation horizontal
workspace_layout tabbed
# workspace_layout stacking
focus_follows_mouse no

# for i3-gaps
#gaps inner 3
#gaps outer 3

#default_border normal|none|pixel
#default_border normal|pixel <px>
#default_floating_border normal|none|pixel
#default_floating_border normal|pixel <px>

# default_border normal/none/pixel
default_border pixel 1

# default_floating_border normal/none/pixel
default_floating_border pixel 1

# hide_edge_borders none|vertical|horizontal|both|smart
hide_edge_borders both

# window colours:           border     background     text
client.focused              #663D40    #663D40        #E4E4EE 
client.focused_inactive     #362728    #362728        #E4E4EE
client.unfocused            #2F393F    #2F393F        #E4E4EE
client.urgent               #C5292F    #C5292F        #E4E4EE

# font for window titles. ISO 10646 = Unicode
# font -xos4-terminus-medium-r-normal-*-12-120-72-72-c-60-iso10646-1
# font -xos4-terminus-medium-r-normal-*-14-140-72-72-c-80-iso8859-1
# font pango: DejaVu Sans Mono, Terminus Regular Semi-Condensed 10
# font pango: AurulentSansMono Nerd Font 10
# font pango: Iosevka Regular 11
font pango: SF Pro Display Regular 11

# Use Mouse+$mod to drag floating windows to their wanted position
# floating_modifier $mod
floating_modifier $ALT

# start a terminal
bindsym $mod+Return exec uxterm
bindsym $mod+Shift+Return exec st
bindsym $ALT+F1 exec urxvt

# start dmenu (a program launcher)
# bindsym $ALT+F2 exec dmenu_run -nb "#2E2E2E" -nf "#CFCFCF" -sb "#387070" -sf "#F5F5FF" 
bindsym $ALT+F2 exec rofi -show

# kill focused window
bindsym $ALT+F4 kill

bindsym $mod+XF86HomePage exec wmctrl -a firefox || firefox
# bindsym XF86AudioNext exec wmctrl -a firefox || firefox

bindsym $mod+XF86Mail exec wmctrl -a thunderbird || thunderbird
# bindsym XF86AudioPrev exec wmctrl -a thunderbird || thunderbird

bindsym $mod+Control+l exec wmctrl -a subl3 || subl3
bindsym Menu exec thunar
bindsym $mod+Control+g exec gimp
bindsym $mod+Control+Print exec "scrot -q 100 -c -d 5 ~/pictures/scrot-shot-%e_%b_%s.jpg"
bindsym $mod+x exec "jgmenu_run"

bindsym XF86AudioRaiseVolume exec "amixer -q sset PCM 2%+ unmute"
bindsym XF86AudioLowerVolume exec "amixer -q sset PCM 2%- unmute"
bindsym XF86AudioMute exec "amixer sset Master,0 toggle"

#bindsym XF86AudioRaiseVolume exec "pamixer -i 5"
#bindsym XF86AudioLowerVolume exec "pamixer -d 5"
#bindsym XF86AudioMute exec "pamixer -t"

bindsym XF86AudioNext exec "mocp --next"
bindsym XF86AudioPrev exec "mocp --previous"
bindsym XF86AudioPlay exec "mocp --toggle-pause"

bindsym $mod+Control+p exec "libreoffice --calc"
bindsym $mod+Control+o exec "libreoffice --writer"
bindsym XF86MonBrightnessUp exec "acpilight -inc 5"
bindsym XF86MonBrightnessDown exec "acpilight -dec 5"

# scratchpad
bindsym $mod+z move scratchpad
bindsym mod4+Shift+z scratchpad show

# assign [class="Navigator"] 2
for_window [class="Navigator"] border pixel 2
# assign [class="Firefox"] 2
for_window [class="Firefox"] border pixel 2
assign [class="Chromium"] 2
for_window [class="Chromium"] border pixel 2
# assign [class="Thunderbird"] 2
for_window [class="Thunderbird"] border pixel 2
assign [class="Gimp"] 3
for_window [class="Gimp"] border pixel 2
assign [class="Gnome-mplayer"] 4
for_window [class="Gnome-mplayer"] floating enable
for_window [class="Gcolor2"] floating enable
#for_window [class="UXTerm"] floating enable, border pixel 2
#for_window [class="XTerm"] floating enable, border pixel 2
#for_window [class="Sakura"] floating enable, border pixel 2
#for_window [class="xfce4-terminal"] floating enable, border pixel 2
for_window [class="XCalc"] floating enable, border pixel 2
for_window [class="Galculator"] floating enable, border pixel 2
for_window [class="XFontSel"] floating enable, border pixel 2
for_window [class="Event Tester"] floating enable, border pixel 2
for_window [class="Gxine"] floating enable, border pixel 2
for_window [class="wm-exit"] floating enable, border pixel 2
for_window [class="void-logout"] floating enable, border pixel 2
for_window [class="void-session-manager"] floating enable, border pixel 2
for_window [class="Exit_Session"] floating enable, border pixel 2
for_window [class="mpv"] floating enable, border pixel 2
for_window [class="SThermal"] floating enable, border pixel 2
for_window [class="PCalendar"] floating enable, border pixel 2
for_window [class="ColorBox"] floating enable, border pixel 2
for_window [class="YadCal"] floating enable, border pixel 2
for_window [class="FontBox"] floating enable, border pixel 2
for_window [class="Connman-gtk"] floating enable, border pixel 2
for_window [class="Yad-icon-browser"] floating enable, border pixel 2
for_window [class="xst-256color"] floating enable, border pixel 2
for_window [class="feh"] floating enable, border pixel 2
for_window [class="Audacious"] floating enable, border pixel 2

# change focus
#bindsym $mod+j focus left
#bindsym $mod+k focus down
#bindsym $mod+l focus up
#bindsym $mod+semicolon focus right

bindsym $ALT+Tab focus right
bindsym $ALT+Shift+Tab focus left

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+H move left
bindsym $mod+Shift+J move down
bindsym $mod+Shift+K move up
bindsym $mod+Shift+L move right

# alternatively, you can use the cursor keys:
bindsym $ALT+Shift+Left move left
bindsym $ALT+Shift+Down move down
bindsym $ALT+Shift+Up move up
bindsym $ALT+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $ALT+F11 fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+s layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+b layout default
bindsym $mod+f floating toggle

# toggle tiling / floating
bindsym $mod+Shift+f floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+p focus parent

# focus the child container
bindsym $mod+c focus child

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace - note it is mod+shift+1 etc.
bindsym $mod+Shift+exclam move container to workspace 1
bindsym $mod+Shift+at move container to workspace 2
bindsym $mod+Shift+numbersign move container to workspace 3
bindsym $mod+Shift+dollar move container to workspace 4
bindsym $mod+Shift+percent move container to workspace 5
bindsym $mod+Shift+asciicircum move container to workspace 6
bindsym $mod+Shift+ampersand move container to workspace 7
bindsym $mod+Shift+asterisk move container to workspace 8
bindsym $mod+Shift+parenleft move container to workspace 9
bindsym $mod+Shift+parenright move container to workspace 10

# reload the configuration file
bindsym $mod+Shift+C reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+q restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+Q exec rofi-logout
bindsym $mod+Shift+e exit

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym h resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        # bindsym Left resize shrink width 10 px or 10 ppt
        # bindsym Down resize grow height 10 px or 10 ppt
        # bindsym Up resize shrink height 10 px or 10 ppt
        # bindsym Right resize grow width 10 px or 10 ppt
	
        # same bindings, but for the arrow keys
        bindsym $mod+Shift+Left resize shrink width 10 px or 10 ppt
        bindsym $mod+Shift+Down resize grow height 10 px or 10 ppt
        bindsym $mod+Shift+Up resize shrink height 10 px or 10 ppt
        bindsym $mod+Shift+Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
#

bar {
       position top
       output LVDS
       workspace_buttons yes
#      tray_output none|primary|<output>
       tray_output primary
       tray_padding 1
       separator_symbol "  "
       bindsym --release button2 exec --no-startup-id import -window root -quality 100 ~/pictures/i3screen-import-`date +%F_%s`.png
       bindsym --release button3 exec jgmenu_run
#       font pango: mononoki Nerd Font 10
#       font -xos4-terminus-medium-r-normal-*-14-140-72-72-c-80-iso10646-1
       font pango: SF Mono Regular 11
       colors {
          background #1B1B1B
          statusline #E4E4EE
          focused_workspace   #663D40 #663D40 #E4E4EE
          active_workspace    #2F393F #2F393F #E4E4EE
          inactive_workspace  #2F393F #2F393F #E4E4EE
          urgent_workspace    #C5292F #C5292F	#E4E4EE
     }
#      status_command "$HOME/.config/i3/i3conkybar.sh"
#      status_command "slstatus -s"
      status_command i3blocks
#      status_command i3status
}
